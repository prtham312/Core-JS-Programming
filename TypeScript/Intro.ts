function add_number(num : number) : number{
    
    return num+3+3
}
add_number(3.2)

// testing data types assigned by type script itself
function hero(num : string) : number {
    return 3
}

//for void function
function error(errmsg : string) : void{
    console.log(errmsg)
}

export{}